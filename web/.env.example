# Canonical production origin, without a trailing slash.
# Required for absolute canonical URLs, social previews, robots sitemap links, and sitemap.xml.
NEXT_PUBLIC_SITE_URL=https://your-production-domain.example
